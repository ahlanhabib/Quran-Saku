rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
    match /pushSubscriptions/{subId} {
      // Allow the app to read and write push subscriptions
      allow read, write: if true;
    }
  }
}
